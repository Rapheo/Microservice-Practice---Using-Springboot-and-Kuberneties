spring:
  datasource:
    url: jdbc:h2:mem:ORDER_DETAILS;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create-drop  # use create or create-drop for tests

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-3ipx01b1.auth0.com/


microservices:
  product: http://product-service-svc/product/
  payment: http://payment-service-svc/payment/